apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: please-scan-register-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleaseScanRegisterDomain }}
  rules:
    - host: {{ .Values.pleaseScanRegisterDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: please-scan-{{ .Values.envGroup }}-register
                port:
                  number: 80
